# 小売販促ニュースまとめ — 日次更新の手順書

このリポジトリは、関東の主要スーパー13社+全国のこだわりローカル企業+SNS話題を
毎日1ページにまとめて GitHub Pages で公開するサイトです。

公開URL: https://tsujido1981.github.io/hansoku-news/

**このファイルの目的**: 会話の文脈を持たない新規セッション(ルーティンの自動実行など)でも、
このファイルだけを読めば当日分の更新を完遂できるようにすること。

---

## 0. 最初に必ず: 日付は日本時間で判定する(重要)

サーバーのシステム時刻は **UTC** です。そのまま使うと日本の朝5時台は
「UTCではまだ前日」なので、**前日のページを当日分として作ってしまう事故**が起きます
(2026年8月1日 5:14 JST の自動実行が「7月31日」のページを作成した実績あり)。

作業開始時に必ず次のコマンドで日本時間の日付を取得し、それを「今日」として扱うこと。

```bash
TZ=Asia/Tokyo date "+%Y-%m-%d (%a)"
```

`date` を素で実行した結果や、コミット履歴の UTC 表記をそのまま使ってはいけません。

---

## 1. 毎日やること(要約)

1. 上記0章の方法で**日本時間の今日の日付**を確定する
2. `git fetch origin main` して最新化する
3. 今日の日付のページ `YYYY-MM-DD.html` が既にあるか確認する
   - **無い** → 前日のページをコピーして当日版を作る(下記2章)
   - **ある** → 既に誰かが作成済み。バズ3欄の日付が当日かを確認し、古ければ当日版に更新する
4. `index.html` は**常に当日ページと同一内容**にする(アーカイブ節を含めて完全一致)
5. push前に必ず `git fetch` して衝突がないか確認してから push する

---

## 2. 当日ページの作り方

**前日のページをコピーして差分を当てる**方式です。ゼロから書かないこと。

```bash
# 例: 7/28分を作る場合
cp 2026-07-27.html 2026-07-28.html
```

そのうえで以下を必ず更新します。

### 2-1. 日付3か所(必須)

| 箇所 | 例 |
|---|---|
| `<title>` | `小売販促ニュースまとめ 2026年7月28日` |
| ヘッダーの収集分表記 | `2026年7月28日(火) 収集分 \| 関東主要チェーン + 全国こだわりローカル企業` |
| `<footer>` | `小売販促ニュースまとめ \| 2026年7月28日収集` |

祝日・特異日は括弧書きを付ける(例: `2026年7月26日(日・土用の丑の日) 収集分`)。

### 2-2. 「本日」「明日」表記の棚卸し(最重要・事故が起きやすい)

前日ページには「本日◯日」「明日◯日」といった**当日限定の表記**が必ず残っています。
`grep -n "本日\|明日" <前日ファイル>` で全部洗い出し、1件ずつ次のように直します。

- 昨日が「本日」だったもの → 日付表記に戻す(例: 「本日7月24日オープン」→「7月24日オープン」)
- 昨日「明日」だったもの → 「本日」に昇格(例: 「明日7月24日開店予定」→「本日7月24日開店」)
- 曜日固定の企画は当日の曜日に合わせる
  - 月曜: 京急ストア「京急プレミアポイント5倍デー」
  - 火曜: サミット「ポイント10倍デー」
  - 土曜: 東急ストア「スマート払い限定ポイント5倍デー」
  - 日曜: まいばすけっと「WAON POINT 5倍」
- 毎月固定日: ロピア「ゾロ目Day」(11日・22日)、東急ストア「19日ポイントアップデー」、
  京急ストア「3の日」(3日)・「快特日」(15日)
- 締切が過ぎたキャンペーンは削除するか「終了」と明記する

### 2-3. ローカル企業欄(毎日3社・日替わり)

3枚目のカードだけ見出しに `(今日の発掘)` を付けます。

**直近5日分と重複しない3社**を選ぶこと。過去のカードは archive の HTML から
そのままコピーして流用できます(内容が古くなければ)。

これまで登場した企業(重複回避の参考):
いかりスーパー / いちやまマート / ぎゅーとら / とりせん / どんたく / ウジエスーパー /
オオゼキ / サポーレ / スーパーまるおか / スーパーマーケットハロー / ダイキョーバリュー /
ダイワスーパー / ツルヤ / ハローデイ / フレンドフーズ / フードストアあおき / マルイチ /
ヤマダストアー / 主婦の店 さいち / 北野エース / 田子重 / 福島屋 / 紀ノ國屋 / 遠鉄ストア /
阪食(阪急オアシス)

### 2-4. 各社の最新情報を Web 検索で更新

関東13社(京急ストア・サミット・ライフ・ヤオコー・ロピア・成城石井・オーケー・
マルエツ・いなげや・カスミ・まいばすけっと・東急ストア・マミーマート)について、
新店・リニューアル・キャンペーン・新商品を検索し、確認できたものを反映します。

**出典が確認できたものだけ**書くこと。リンクは `.links` に追加します。

---

## 3. SNS 3欄のルール(最重要)

### 3-1. 掲載基準

| 欄 | 基準 |
|---|---|
| SNSでバズった食の話題(X) | 100リポスト以上 **または** 1,000いいね以上 **または** 1万閲覧以上 |
| 急上昇ウォッチ | 24時間以内に200リポスト以上 **または** 500いいね以上、またはX/Googleの急上昇ワード入り。「火元(きっかけ)」を必ず記載 |
| 京急ストア・元町ユニオン ウォッチ | 過去48時間以内に3リポスト以上 **または** 3いいね以上 |

対象は食品・飲料の「商品」とスーパー/コンビニ/メーカーの話題。
**レストラン・飲食店のメニューは除外**(参考掲載する場合はその旨を明記)。
同じ投稿は**初出の日のみ**掲載する日替わり方式。

### 3-2. 絶対に守るルール

> **数値の推測・創作は絶対にしない。**

実数(リポスト/いいね/表示)を確認できない場合は、数字を書かずに
「実数未確認のため掲載保留」として話題だけ記録します。これは本サイトの信頼性の根幹です。

### 3-3. 実行環境による違いと、数値の取り方(重要)

2026年8月1日に、クラウド環境のネットワークポリシーを **Custom** にして
`x.com` `*.x.com` `api.x.com` `cdn.syndication.twimg.com` `*.twimg.com` `twitter.com`
`search.yahoo.co.jp` `togetter.com` を許可しました(「一般的なパッケージマネージャーの
デフォルトリストも含める」はONのまま)。これにより取得できる数値が変わっています。

#### 何が取れて、何が取れないか(実測済み)

| 項目 | 可否 | 備考 |
|---|---|---|
| **いいね数** | **取れる** | 掲載基準の「1,000いいね以上」は実数で判定できる |
| **返信数** | **取れる** | 参考情報として記載可 |
| リポスト数 | 取れない | 「100リポスト以上」の基準は判定不可 |
| 表示数(インプレッション) | 取れない | 「1万閲覧以上」の基準は判定不可 |
| **X内のキーワード検索** | **できない** | バズっている投稿を自力で探すことはできない |

#### 実際の手順(この流れで運用する)

1. **話題を見つける**: Web検索・ニュース記事・まとめサイトで、話題になっている
   投稿やその発信元(公式アカウントの告知など)を特定する
2. **投稿URL(ID)を得る**: 記事中のリンクなどから対象ポストのIDを取得する
3. **数値を裏取りする**: 以下で いいね数・返信数 を確認する

```bash
curl -sS "https://cdn.syndication.twimg.com/tweet-result?id=<ポストID>&lang=ja&token=x" | \
  python3 -c "import sys,json;d=json.load(sys.stdin);print('いいね:',d.get('favorite_count'),'返信:',d.get('conversation_count'))"
```

4. **掲載可否を判定する**
   - **いいね数が1,000以上** → 実数付きで**正式掲載**する(いいね数と返信数のみ記載)
   - **いいね数が1,000未満** → リポスト数・表示数では基準を満たすかもしれないが
     **確認できないので掲載保留**にする(数字は書かない)

#### 注意

- 環境設定の変更は**変更後に開始したセッションから有効**です。実行中セッションには反映されません
- リポスト数・表示数を「たぶんこれくらい」と補完してはいけません(3-2章の原則)
- TLS検証の無効化やプロキシ迂回は**禁止**です
- ネットワーク未許可の環境で実行した場合は、従来どおり報道ベースで「掲載保留」にし、
  注記欄(`.note`)にその旨を明記します

---

## 4. アーカイブと index.html

- アーカイブ節(`<h2>アーカイブ(過去のまとめ)</h2>`)の先頭に当日分のリンクを追加する
- `index.html` は当日ページの**完全なコピー**にする
  ```bash
  cp 2026-07-28.html index.html
  diff 2026-07-28.html index.html   # 差分ゼロを確認
  ```

---

## 5. Git 運用

```bash
# 1. 作業前に必ず最新化
git fetch origin main && git merge origin/main --ff-only

# 2. 作業後、push直前にもう一度リモート確認(PC側と衝突しやすい)
git fetch origin main
git log HEAD..origin/main --oneline   # 空でなければ相手が先に push している

# 3. 空なら push
git add <当日ファイル> index.html
git commit -m "販促ニュース YYYY-MM-DD"
git push origin main
```

### 衝突したとき

PC側とクラウド側が同時に動くと衝突します。その場合は:

1. **PC側の版を正とする**(実数付きバズ欄を持っているため価値が高い)
2. `git reset --hard origin/main` でPC版を採用
3. 自分の版にしか無い**裏取り済みの情報だけ**を上から追記して push する

`index.html` は必ず当日ページと同一に保つこと。

---

## 6. 現在の自動実行体制

| 実行主体 | タイミング | 特徴 |
|---|---|---|
| PC (Windowsタスクスケジューラ + Claude Code headless) | 毎朝5時台 | X内検索が使える(Chrome/X連携)ので、投稿を自力で発見できる。リポスト数・表示数も見える。**PCの電源が入っていないと実行されない** |
| claude.ai ルーティン (クラウド、環境=Custom) | 設定した時刻 | PC不要で確実に動く。2026年8月1日よりネットワークポリシーで x.com 系を許可済み。**ただしX内検索はできない**(投稿URLが分かっている場合のみ、いいね数・返信数を裏取り可能。3-3章参照) |

両方が動いた日は衝突するので、上記5章の手順で統合します。
ルーティンはPC側(5時台)を避けた時刻に設定してください(推奨: 6:30 JST)。

**環境が Custom になっていない別のクラウドセッション**(新規ルーティンや別環境)では
x.com 系が引き続き403で遮断されます。その場合は3-3章の「未許可の場合」の手順(報道ベース・掲載保留)に従ってください。

---

## 7. やってはいけないこと

- 実数(リポスト/いいね/表示)を推測・創作して書く
- 出典が確認できない情報を断定的に書く
- `index.html` と当日ページの内容を食い違わせる
- push前のリモート確認を省略する(衝突・上書き事故の原因)
- TLS検証の無効化や `HTTPS_PROXY` の解除でネットワーク制限を回避しようとする
